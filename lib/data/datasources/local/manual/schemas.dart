class SchemaDefinitions {
  static List<String> get allTables => [
    ..._coreTables,
    ..._productTables,
    ..._customerTables,
    ..._supplierTables,
    ..._salesTables,
    ..._purchaseTables,
    ..._inventoryTables,
    ..._accountingTables,
    ..._financialTables,
    ..._hrTables,
    ..._attendanceLeaveTables,
    ..._manufacturingTables,
    ..._logisticsTables,
    ..._pricingTables,
    ..._fixedAssetsTables,
    ..._taxSerialTables,
    ..._proformaTables,
    ..._securityTables,
    ..._currencyTables,
    ..._commissionTables,
    ..._advancedTables,
    ..._zakatEosbTables,
    ..._appConfigTables,
  ];

  static List<String> get allIndexes => _performanceIndexes;

  // ===================== CORE =====================
  static final List<String> _coreTables = [
    '''CREATE TABLE IF NOT EXISTS branches (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      code TEXT UNIQUE NOT NULL,
      address TEXT,
      phone TEXT,
      is_active INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1
    )''',
    '''CREATE TABLE IF NOT EXISTS users (
      id TEXT PRIMARY KEY,
      username TEXT UNIQUE NOT NULL,
      password TEXT NOT NULL,
      role TEXT NOT NULL,
      full_name TEXT NOT NULL,
      password_hash TEXT,
      password_salt TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS permissions (
      id TEXT PRIMARY KEY,
      code TEXT UNIQUE NOT NULL,
      description TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS role_permissions (
      id TEXT PRIMARY KEY,
      role TEXT NOT NULL,
      permission_code TEXT NOT NULL REFERENCES permissions(code),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sync_queue (
      id TEXT PRIMARY KEY,
      entity_table TEXT NOT NULL,
      entity_id TEXT NOT NULL,
      operation TEXT NOT NULL,
      payload TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      status INTEGER DEFAULT 0,
      device_id TEXT,
      version INTEGER DEFAULT 1,
      retry_count INTEGER DEFAULT 0,
      last_error TEXT
    )''',
    '''CREATE TABLE IF NOT EXISTS audit_logs (
      id TEXT PRIMARY KEY,
      user_id TEXT,
      action TEXT NOT NULL,
      target_entity TEXT NOT NULL,
      entity_id TEXT NOT NULL,
      details TEXT,
      timestamp TEXT DEFAULT (datetime('now')),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== PRODUCTS =====================
  static final List<String> _productTables = [
    '''CREATE TABLE IF NOT EXISTS categories (
      id TEXT PRIMARY KEY,
      name TEXT UNIQUE NOT NULL,
      code TEXT UNIQUE,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS products (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      sku TEXT UNIQUE NOT NULL,
      barcode TEXT UNIQUE,
      category_id TEXT REFERENCES categories(id),
      unit TEXT DEFAULT 'pcs',
      carton_unit TEXT DEFAULT 'carton',
      pieces_per_carton INTEGER DEFAULT 1,
      kilo_unit TEXT,
      box_unit TEXT,
      buy_price TEXT DEFAULT '0',
      sell_price TEXT DEFAULT '0',
      wholesale_price TEXT DEFAULT '0',
      stock TEXT DEFAULT '0',
      max_stock TEXT DEFAULT '1000',
      supplier_id TEXT REFERENCES suppliers(id),
      valuation_method TEXT DEFAULT 'FIFO',
      allow_free_qty INTEGER DEFAULT 0,
      is_service INTEGER DEFAULT 0,
      alert_limit TEXT DEFAULT '10',
      expiry_date TEXT,
      tax_rate TEXT DEFAULT '0',
      is_active INTEGER DEFAULT 1,
      parent_product_id TEXT REFERENCES products(id),
      attributes TEXT,
      additional_cost TEXT,
      image_path TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS product_units (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      unit_name TEXT NOT NULL,
      barcode TEXT UNIQUE,
      unit_factor TEXT DEFAULT '1',
      buy_price TEXT,
      sell_price TEXT,
      wholesale_price TEXT,
      half_wholesale_price TEXT,
      is_default INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS global_units (
      id TEXT PRIMARY KEY,
      name TEXT UNIQUE NOT NULL,
      symbol TEXT,
      is_custom INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS unit_conversions (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      unit_name TEXT NOT NULL,
      factor TEXT DEFAULT '1',
      is_base_unit INTEGER DEFAULT 0,
      buy_price TEXT,
      sell_price TEXT,
      barcode TEXT UNIQUE,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS item_variants (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      attribute_name TEXT NOT NULL DEFAULT '',
      attribute_value TEXT NOT NULL DEFAULT '',
      additional_price TEXT DEFAULT '0',
      sku TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS price_history (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      old_price TEXT DEFAULT '0',
      new_price TEXT DEFAULT '0',
      type TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== CUSTOMERS =====================
  static final List<String> _customerTables = [
    '''CREATE TABLE IF NOT EXISTS customers (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      normalized_name TEXT,
      phone TEXT,
      tax_number TEXT,
      address TEXT,
      email TEXT,
      customer_type TEXT DEFAULT 'RETAIL',
      is_active INTEGER DEFAULT 1,
      credit_limit TEXT DEFAULT '0',
      balance TEXT DEFAULT '0',
      account_id TEXT REFERENCES gl_accounts(id),
      currency_id TEXT REFERENCES currencies(id),
      exchange_rate TEXT DEFAULT '1',
      is_quick_customer INTEGER DEFAULT 0,
      created_from_pos INTEGER DEFAULT 0,
      discount_rate TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS customer_payments (
      id TEXT PRIMARY KEY,
      customer_id TEXT NOT NULL REFERENCES customers(id),
      amount TEXT NOT NULL,
      payment_date TEXT DEFAULT (datetime('now')),
      note TEXT,
      payment_method TEXT DEFAULT 'cash',
      reference_number TEXT,
      account_id TEXT REFERENCES gl_accounts(id),
      status TEXT DEFAULT 'COMPLETED',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS customer_payment_links (
      id TEXT PRIMARY KEY,
      payment_id TEXT NOT NULL REFERENCES customer_payments(id),
      sale_id TEXT NOT NULL REFERENCES sales(id),
      amount TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS ar_invoices (
      id TEXT PRIMARY KEY,
      customer_id TEXT NOT NULL REFERENCES customers(id),
      invoice_number TEXT NOT NULL,
      invoice_date TEXT DEFAULT (datetime('now')),
      due_date TEXT,
      total_amount INTEGER NOT NULL,
      tax_amount TEXT DEFAULT '0',
      paid_amount TEXT DEFAULT '0',
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      account_id TEXT REFERENCES gl_accounts(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS credit_notes (
      id TEXT PRIMARY KEY,
      customer_id TEXT REFERENCES customers(id),
      sale_id TEXT REFERENCES sales(id),
      amount TEXT DEFAULT '0',
      reason TEXT,
      status TEXT DEFAULT 'DRAFT',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS credit_note_items (
      id TEXT PRIMARY KEY,
      credit_note_id TEXT NOT NULL REFERENCES credit_notes(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      price TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== SUPPLIERS =====================
  static final List<String> _supplierTables = [
    '''CREATE TABLE IF NOT EXISTS suppliers (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      phone TEXT,
      contact_person TEXT,
      tax_number TEXT,
      address TEXT,
      email TEXT,
      supplier_type TEXT DEFAULT 'LOCAL',
      is_active INTEGER DEFAULT 1,
      balance TEXT DEFAULT '0',
      account_id TEXT REFERENCES gl_accounts(id),
      credit_limit TEXT DEFAULT '0',
      currency_id TEXT REFERENCES currencies(id),
      exchange_rate TEXT DEFAULT '1',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS supplier_payments (
      id TEXT PRIMARY KEY,
      supplier_id TEXT NOT NULL REFERENCES suppliers(id),
      amount TEXT NOT NULL,
      remaining_amount TEXT DEFAULT '0',
      payment_date TEXT DEFAULT (datetime('now')),
      note TEXT,
      payment_method TEXT DEFAULT 'cash',
      reference_number TEXT,
      account_id TEXT REFERENCES gl_accounts(id),
      status TEXT DEFAULT 'COMPLETED',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_payment_links (
      id TEXT PRIMARY KEY,
      payment_id TEXT NOT NULL REFERENCES supplier_payments(id),
      purchase_id TEXT NOT NULL REFERENCES purchases(id),
      amount TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS ap_invoices (
      id TEXT PRIMARY KEY,
      supplier_id TEXT NOT NULL REFERENCES suppliers(id),
      invoice_number TEXT NOT NULL,
      invoice_date TEXT DEFAULT (datetime('now')),
      due_date TEXT,
      total_amount INTEGER NOT NULL,
      tax_amount TEXT DEFAULT '0',
      paid_amount TEXT DEFAULT '0',
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      account_id TEXT REFERENCES gl_accounts(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== SALES =====================
  static final List<String> _salesTables = [
    '''CREATE TABLE IF NOT EXISTS sales (
      id TEXT PRIMARY KEY,
      customer_id TEXT REFERENCES customers(id),
      total TEXT NOT NULL,
      discount TEXT DEFAULT '0',
      tax TEXT DEFAULT '0',
      payment_method INTEGER NOT NULL,
      is_credit INTEGER DEFAULT 0,
      status INTEGER DEFAULT 0,
      sale_type TEXT DEFAULT 'retail',
      currency_id TEXT,
      exchange_rate TEXT DEFAULT '1',
      shipping_cost TEXT DEFAULT '0',
      other_expenses TEXT DEFAULT '0',
      warehouse_id TEXT REFERENCES warehouses(id),
      representative_id TEXT,
      exchange_date TEXT,
      qr_code TEXT,
      hash TEXT,
      signature TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sale_items (
      id TEXT PRIMARY KEY,
      sale_id TEXT NOT NULL REFERENCES sales(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT NOT NULL,
      price TEXT NOT NULL,
      unit_id TEXT REFERENCES global_units(id),
      unit_name TEXT DEFAULT 'حبة',
      unit_factor TEXT DEFAULT '1',
      warehouse_id TEXT REFERENCES warehouses(id),
      batch_id TEXT REFERENCES product_batches(id),
      cost_center_id TEXT REFERENCES cost_centers(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sales_returns (
      id TEXT PRIMARY KEY,
      sale_id TEXT NOT NULL REFERENCES sales(id),
      amount_returned TEXT DEFAULT '0',
      reason TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sales_return_items (
      id TEXT PRIMARY KEY,
      sales_return_id TEXT NOT NULL REFERENCES sales_returns(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT NOT NULL,
      price TEXT NOT NULL,
      unit_factor TEXT DEFAULT '1',
      batch_id TEXT REFERENCES product_batches(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sales_orders (
      id TEXT PRIMARY KEY,
      customer_id TEXT REFERENCES customers(id),
      total TEXT DEFAULT '0',
      order_number TEXT,
      date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'QUOTATION',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sales_order_items (
      id TEXT PRIMARY KEY,
      order_id TEXT NOT NULL REFERENCES sales_orders(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      price TEXT DEFAULT '0',
      unit_id TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== PURCHASES =====================
  static final List<String> _purchaseTables = [
    '''CREATE TABLE IF NOT EXISTS purchases (
      id TEXT PRIMARY KEY,
      supplier_id TEXT REFERENCES suppliers(id),
      total TEXT NOT NULL,
      tax TEXT DEFAULT '0',
      discount TEXT DEFAULT '0',
      landed_costs TEXT DEFAULT '0',
      shipping_cost TEXT DEFAULT '0',
      other_expenses TEXT DEFAULT '0',
      invoice_number TEXT,
      purchase_type TEXT DEFAULT 'cash',
      date TEXT DEFAULT (datetime('now')),
      time TEXT,
      is_credit INTEGER DEFAULT 0,
      status INTEGER DEFAULT 0,
      warehouse_id TEXT REFERENCES warehouses(id),
      currency_id TEXT,
      exchange_rate TEXT DEFAULT '1',
      notes TEXT,
      reference_document TEXT,
      attachment_path TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_items (
      id TEXT PRIMARY KEY,
      purchase_id TEXT NOT NULL REFERENCES purchases(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      unit_id TEXT,
      unit_factor TEXT DEFAULT '1',
      quantity TEXT NOT NULL,
      quantity_in_base_unit TEXT,
      unit_price TEXT NOT NULL,
      price TEXT NOT NULL,
      discount TEXT DEFAULT '0',
      discount_percent TEXT DEFAULT '0',
      tax TEXT DEFAULT '0',
      tax_percent TEXT DEFAULT '0',
      landed_cost_share TEXT DEFAULT '0',
      batch_id TEXT REFERENCES product_batches(id),
      batch_number TEXT,
      expiry_date TEXT,
      warehouse_id TEXT REFERENCES warehouses(id),
      is_carton INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_returns (
      id TEXT PRIMARY KEY,
      purchase_id TEXT NOT NULL REFERENCES purchases(id),
      amount_returned TEXT DEFAULT '0',
      reason TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_return_items (
      id TEXT PRIMARY KEY,
      purchase_return_id TEXT NOT NULL REFERENCES purchase_returns(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT NOT NULL,
      price TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_orders (
      id TEXT PRIMARY KEY,
      supplier_id TEXT REFERENCES suppliers(id),
      total TEXT DEFAULT '0',
      order_number TEXT,
      date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'QUOTATION',
      warehouse_id TEXT REFERENCES warehouses(id),
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS purchase_order_items (
      id TEXT PRIMARY KEY,
      order_id TEXT NOT NULL REFERENCES purchase_orders(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      price TEXT DEFAULT '0',
      unit_id TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS good_received_notes (
      id TEXT PRIMARY KEY,
      purchase_id TEXT REFERENCES purchases(id),
      supplier_id TEXT REFERENCES suppliers(id),
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      grn_number TEXT UNIQUE NOT NULL,
      received_date TEXT DEFAULT (datetime('now')),
      received_by TEXT,
      notes TEXT,
      status TEXT DEFAULT 'DRAFT',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS good_received_note_items (
      id TEXT PRIMARY KEY,
      grn_id TEXT NOT NULL REFERENCES good_received_notes(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      batch_number TEXT,
      expiry_date TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== INVENTORY =====================
  static final List<String> _inventoryTables = [
    '''CREATE TABLE IF NOT EXISTS warehouses (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      location TEXT,
      account_id TEXT REFERENCES gl_accounts(id),
      branch_id TEXT REFERENCES branches(id),
      is_default INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1
    )''',
    '''CREATE TABLE IF NOT EXISTS product_batches (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      batch_number TEXT NOT NULL,
      expiry_date TEXT,
      quantity TEXT DEFAULT '0',
      initial_quantity TEXT DEFAULT '0',
      cost_price TEXT DEFAULT '0',
      reserved_quantity TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS stock_movements (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      from_warehouse_id TEXT REFERENCES warehouses(id),
      to_warehouse_id TEXT REFERENCES warehouses(id),
      quantity TEXT NOT NULL,
      cost TEXT DEFAULT '0',
      batch_id TEXT REFERENCES product_batches(id),
      movement_date TEXT DEFAULT (datetime('now')),
      type TEXT NOT NULL,
      transaction_id TEXT,
      date TEXT,
      reference_id TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS stock_transfers (
      id TEXT PRIMARY KEY,
      from_warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      to_warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      transfer_date TEXT DEFAULT (datetime('now')),
      note TEXT,
      status TEXT DEFAULT 'COMPLETED',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS stock_transfer_items (
      id TEXT PRIMARY KEY,
      transfer_id TEXT NOT NULL REFERENCES stock_transfers(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      batch_id TEXT NOT NULL REFERENCES product_batches(id),
      quantity TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS stock_takes (
      id TEXT PRIMARY KEY,
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'DRAFT',
      note TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS stock_take_items (
      id TEXT PRIMARY KEY,
      stock_take_id TEXT NOT NULL REFERENCES stock_takes(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      expected_qty TEXT DEFAULT '0',
      actual_qty TEXT DEFAULT '0',
      variance TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS inventory_audits (
      id TEXT PRIMARY KEY,
      audit_date TEXT DEFAULT (datetime('now')),
      note TEXT,
      audited_by TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS inventory_audit_items (
      id TEXT PRIMARY KEY,
      audit_id TEXT NOT NULL REFERENCES inventory_audits(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      system_stock TEXT DEFAULT '0',
      actual_stock TEXT DEFAULT '0',
      difference TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS inventory_transactions (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      batch_id TEXT REFERENCES product_batches(id),
      quantity TEXT DEFAULT '0',
      type TEXT NOT NULL,
      reference_id TEXT NOT NULL,
      date TEXT DEFAULT (datetime('now')),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== ACCOUNTING =====================
  static final List<String> _accountingTables = [
    '''CREATE TABLE IF NOT EXISTS gl_accounts (
      id TEXT PRIMARY KEY,
      code TEXT UNIQUE NOT NULL,
      name TEXT NOT NULL,
      account_type INTEGER NOT NULL,
      analytic_type TEXT,
      parent_id TEXT REFERENCES gl_accounts(id),
      is_header INTEGER DEFAULT 0,
      balance TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS cost_centers (
      id TEXT PRIMARY KEY,
      code TEXT UNIQUE NOT NULL,
      name TEXT NOT NULL,
      parent_id TEXT REFERENCES cost_centers(id),
      type TEXT DEFAULT 'department',
      is_active INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS gl_entries (
      id TEXT PRIMARY KEY,
      description TEXT NOT NULL,
      date TEXT DEFAULT (datetime('now')),
      reference_type TEXT,
      reference_id TEXT,
      status TEXT DEFAULT 'DRAFT',
      posted_at TEXT,
      posted_by TEXT,
      currency_id TEXT,
      exchange_rate TEXT DEFAULT '1',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS gl_lines (
      id TEXT PRIMARY KEY,
      entry_id TEXT NOT NULL REFERENCES gl_entries(id),
      account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      cost_center_id TEXT REFERENCES cost_centers(id),
      debit TEXT DEFAULT '0',
      credit TEXT DEFAULT '0',
      currency_id TEXT REFERENCES currencies(id),
      exchange_rate TEXT DEFAULT '1',
      memo TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS accounting_periods (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      fiscal_year INTEGER NOT NULL,
      start_date TEXT NOT NULL,
      end_date TEXT NOT NULL,
      is_closed INTEGER DEFAULT 0,
      closed_at TEXT,
      closed_by TEXT,
      closing_type TEXT,
      status TEXT DEFAULT 'OPEN',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id),
      UNIQUE(fiscal_year, status)
    )''',
    '''CREATE TABLE IF NOT EXISTS account_transactions (
      id TEXT PRIMARY KEY,
      account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      date TEXT DEFAULT (datetime('now')),
      type TEXT NOT NULL,
      reference_id TEXT,
      debit TEXT DEFAULT '0',
      credit TEXT DEFAULT '0',
      reconciled INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS posting_profiles (
      id TEXT PRIMARY KEY,
      operation_type TEXT NOT NULL,
      account_type TEXT NOT NULL,
      account_id TEXT REFERENCES gl_accounts(id),
      description TEXT,
      account_code TEXT,
      is_active INTEGER DEFAULT 1,
      sequence INTEGER DEFAULT 0,
      side TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      branch_id TEXT REFERENCES branches(id),
      sync_status INTEGER DEFAULT 1
    )''',
    '''CREATE TABLE IF NOT EXISTS recurring_entries (
      id TEXT PRIMARY KEY,
      name TEXT,
      description TEXT,
      frequency TEXT,
      next_date TEXT,
      amount TEXT DEFAULT '0',
      account_id TEXT REFERENCES gl_accounts(id),
      is_active INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS recurring_entry_executions (
      id TEXT PRIMARY KEY,
      recurring_entry_id TEXT NOT NULL REFERENCES recurring_entries(id),
      executed_at TEXT DEFAULT (datetime('now')),
      gl_entry_id TEXT,
      status TEXT DEFAULT 'PENDING',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== FINANCIAL =====================
  static final List<String> _financialTables = [
    '''CREATE TABLE IF NOT EXISTS cashbox_transactions (
      id TEXT PRIMARY KEY,
      amount TEXT DEFAULT '0',
      type TEXT NOT NULL,
      category TEXT NOT NULL,
      reference_id TEXT,
      note TEXT,
      user_id TEXT NOT NULL REFERENCES users(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS financial_transfers (
      id TEXT PRIMARY KEY,
      sender_account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      receiver_account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      amount TEXT DEFAULT '0',
      commission TEXT DEFAULT '0',
      company TEXT,
      transfer_type TEXT NOT NULL,
      check_id TEXT REFERENCES checks(id),
      date TEXT DEFAULT (datetime('now')),
      note TEXT,
      status TEXT DEFAULT 'POSTED',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS reconciliations (
      id TEXT PRIMARY KEY,
      account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      date TEXT DEFAULT (datetime('now')),
      book_balance TEXT DEFAULT '0',
      actual_balance TEXT DEFAULT '0',
      difference TEXT DEFAULT '0',
      note TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS reconciliation_details (
      id TEXT PRIMARY KEY,
      reconciliation_id TEXT NOT NULL REFERENCES reconciliations(id),
      transaction_id TEXT NOT NULL REFERENCES account_transactions(id),
      statement_amount TEXT NOT NULL,
      statement_date TEXT NOT NULL,
      reference TEXT,
      branch_id TEXT REFERENCES branches(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1
    )''',
    '''CREATE TABLE IF NOT EXISTS checks (
      id TEXT PRIMARY KEY,
      check_number TEXT NOT NULL,
      bank_name TEXT NOT NULL,
      due_date TEXT NOT NULL,
      amount TEXT DEFAULT '0',
      type TEXT NOT NULL,
      status TEXT DEFAULT 'PENDING',
      partner_id TEXT,
      payment_account_id TEXT REFERENCES gl_accounts(id),
      note TEXT,
      currency_id TEXT REFERENCES currencies(id),
      exchange_rate TEXT DEFAULT '1',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS shifts (
      id TEXT PRIMARY KEY,
      user_id TEXT NOT NULL REFERENCES users(id),
      start_time TEXT DEFAULT (datetime('now')),
      end_time TEXT,
      opening_cash TEXT DEFAULT '0',
      closing_cash TEXT,
      expected_cash TEXT,
      note TEXT,
      is_open INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== HR =====================
  static final List<String> _hrTables = [
    '''CREATE TABLE IF NOT EXISTS employees (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      employee_code TEXT UNIQUE NOT NULL,
      job_title TEXT,
      role TEXT DEFAULT 'USER',
      basic_salary TEXT DEFAULT '0',
      hire_date TEXT,
      warehouse_id TEXT REFERENCES warehouses(id),
      is_active INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS hr_employees (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      employee_code TEXT UNIQUE NOT NULL,
      national_id TEXT,
      phone TEXT,
      email TEXT,
      job_title TEXT,
      department TEXT,
      basic_salary TEXT DEFAULT '0',
      hire_date TEXT,
      contract_type TEXT DEFAULT 'PERMANENT',
      status TEXT DEFAULT 'ACTIVE',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS payroll_entries (
      id TEXT PRIMARY KEY,
      month INTEGER NOT NULL,
      year INTEGER NOT NULL,
      generation_date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'DRAFT',
      note TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS payroll_lines (
      id TEXT PRIMARY KEY,
      payroll_entry_id TEXT NOT NULL REFERENCES payroll_entries(id),
      employee_id TEXT NOT NULL REFERENCES employees(id),
      basic_salary TEXT DEFAULT '0',
      allowances TEXT DEFAULT '0',
      deductions TEXT DEFAULT '0',
      net_salary TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS hr_payroll_runs (
      id TEXT PRIMARY KEY,
      month INTEGER NOT NULL,
      year INTEGER NOT NULL,
      run_date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'DRAFT',
      total_gross TEXT DEFAULT '0',
      total_deductions TEXT DEFAULT '0',
      total_net TEXT DEFAULT '0',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS hr_payroll_details (
      id TEXT PRIMARY KEY,
      payroll_run_id TEXT NOT NULL REFERENCES hr_payroll_runs(id),
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      basic_salary TEXT DEFAULT '0',
      allowances TEXT DEFAULT '0',
      deductions TEXT DEFAULT '0',
      net_pay TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS hr_additional_deductions (
      id TEXT PRIMARY KEY,
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      amount TEXT DEFAULT '0',
      type TEXT NOT NULL,
      description TEXT,
      date TEXT DEFAULT (datetime('now')),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== ATTENDANCE & LEAVE =====================
  static final List<String> _attendanceLeaveTables = [
    '''CREATE TABLE IF NOT EXISTS attendance_records (
      id TEXT PRIMARY KEY,
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      date TEXT NOT NULL,
      check_in TEXT,
      check_out TEXT,
      status TEXT DEFAULT 'PRESENT',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS leave_types (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      days_allowed INTEGER DEFAULT 0,
      is_paid INTEGER DEFAULT 1,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS leave_requests (
      id TEXT PRIMARY KEY,
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      leave_type_id TEXT NOT NULL REFERENCES leave_types(id),
      start_date TEXT NOT NULL,
      end_date TEXT NOT NULL,
      days_count INTEGER NOT NULL,
      status TEXT DEFAULT 'PENDING',
      reason TEXT,
      approved_by TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS leave_balances (
      id TEXT PRIMARY KEY,
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      leave_type_id TEXT NOT NULL REFERENCES leave_types(id),
      year INTEGER NOT NULL,
      total_days INTEGER DEFAULT 0,
      used_days INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== MANUFACTURING =====================
  static final List<String> _manufacturingTables = [
    '''CREATE TABLE IF NOT EXISTS bill_of_materials (
      id TEXT PRIMARY KEY,
      finished_product_id TEXT NOT NULL REFERENCES products(id),
      component_product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS production_orders (
      id TEXT PRIMARY KEY,
      finished_product_id TEXT NOT NULL REFERENCES products(id),
      planned_quantity TEXT DEFAULT '0',
      actual_quantity TEXT DEFAULT '0',
      date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'PLANNED',
      warehouse_id TEXT REFERENCES warehouses(id),
      note TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS production_order_items (
      id TEXT PRIMARY KEY,
      production_order_id TEXT NOT NULL REFERENCES production_orders(id),
      component_product_id TEXT NOT NULL REFERENCES products(id),
      planned_quantity TEXT DEFAULT '0',
      actual_quantity TEXT DEFAULT '0',
      unit_cost TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== LOGISTICS =====================
  static final List<String> _logisticsTables = [
    '''CREATE TABLE IF NOT EXISTS delivery_notes (
      id TEXT PRIMARY KEY,
      sale_order_id TEXT REFERENCES sales_orders(id),
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      delivery_number TEXT UNIQUE NOT NULL,
      delivery_date TEXT DEFAULT (datetime('now')),
      delivered_by TEXT,
      notes TEXT,
      status TEXT DEFAULT 'DRAFT',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS delivery_note_items (
      id TEXT PRIMARY KEY,
      delivery_note_id TEXT NOT NULL REFERENCES delivery_notes(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      batch_id TEXT REFERENCES product_batches(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== PRICING =====================
  static final List<String> _pricingTables = [
    '''CREATE TABLE IF NOT EXISTS price_lists (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      currency TEXT DEFAULT 'USD',
      is_active INTEGER DEFAULT 1,
      description TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS price_list_items (
      id TEXT PRIMARY KEY,
      price_list_id TEXT NOT NULL REFERENCES price_lists(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      price TEXT DEFAULT '0',
      min_quantity TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS promotions (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      type TEXT NOT NULL,
      value TEXT DEFAULT '0',
      start_date TEXT NOT NULL,
      end_date TEXT NOT NULL,
      is_active INTEGER DEFAULT 1,
      category_id TEXT REFERENCES categories(id),
      product_id TEXT REFERENCES products(id),
      min_purchase_amount TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== FIXED ASSETS =====================
  static final List<String> _fixedAssetsTables = [
    '''CREATE TABLE IF NOT EXISTS acc_asset_categories (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      depreciation_method TEXT DEFAULT 'STRAIGHT_LINE',
      useful_life_years INTEGER DEFAULT 5,
      depreciation_rate TEXT DEFAULT '0',
      account_id TEXT REFERENCES gl_accounts(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS fixed_assets (
      id TEXT PRIMARY KEY,
      asset_category_id TEXT REFERENCES acc_asset_categories(id),
      code TEXT UNIQUE NOT NULL,
      name TEXT NOT NULL,
      purchase_date TEXT,
      purchase_cost TEXT DEFAULT '0',
      current_value TEXT DEFAULT '0',
      salvage_value TEXT DEFAULT '0',
      useful_life_years INTEGER DEFAULT 5,
      depreciation_method TEXT DEFAULT 'STRAIGHT_LINE',
      depreciation_rate TEXT DEFAULT '0',
      accumulated_depreciation TEXT DEFAULT '0',
      status TEXT DEFAULT 'ACTIVE',
      location TEXT,
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS acc_asset_depreciation_logs (
      id TEXT PRIMARY KEY,
      asset_id TEXT NOT NULL REFERENCES fixed_assets(id),
      period TEXT NOT NULL,
      amount TEXT DEFAULT '0',
      book_value_before TEXT DEFAULT '0',
      book_value_after TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS acc_asset_disposals (
      id TEXT PRIMARY KEY,
      asset_id TEXT NOT NULL REFERENCES fixed_assets(id),
      disposal_date TEXT NOT NULL,
      disposal_type TEXT NOT NULL,
      proceeds TEXT DEFAULT '0',
      gain_loss TEXT DEFAULT '0',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== TAX & SERIAL =====================
  static final List<String> _taxSerialTables = [
    '''CREATE TABLE IF NOT EXISTS withholding_tax_entries (
      id TEXT PRIMARY KEY,
      partner_id TEXT,
      partner_type TEXT,
      invoice_id TEXT,
      invoice_type TEXT,
      amount TEXT DEFAULT '0',
      tax_rate TEXT DEFAULT '0',
      tax_amount TEXT DEFAULT '0',
      date TEXT DEFAULT (datetime('now')),
      status TEXT DEFAULT 'DRAFT',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS serial_numbers (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      serial_number TEXT UNIQUE NOT NULL,
      batch_id TEXT REFERENCES product_batches(id),
      warehouse_id TEXT REFERENCES warehouses(id),
      status TEXT DEFAULT 'IN_STOCK',
      sale_id TEXT REFERENCES sales(id),
      purchase_id TEXT REFERENCES purchases(id),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== PROFORMA =====================
  static final List<String> _proformaTables = [
    '''CREATE TABLE IF NOT EXISTS proforma_invoices (
      id TEXT PRIMARY KEY,
      customer_id TEXT REFERENCES customers(id),
      total TEXT DEFAULT '0',
      discount TEXT DEFAULT '0',
      tax TEXT DEFAULT '0',
      valid_until TEXT,
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS proforma_invoice_items (
      id TEXT PRIMARY KEY,
      proforma_invoice_id TEXT NOT NULL REFERENCES proforma_invoices(id),
      product_id TEXT NOT NULL REFERENCES products(id),
      quantity TEXT DEFAULT '0',
      price TEXT DEFAULT '0',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== SECURITY =====================
  static final List<String> _securityTables = [
    '''CREATE TABLE IF NOT EXISTS user_sessions (
      id TEXT PRIMARY KEY,
      user_id TEXT NOT NULL REFERENCES users(id),
      token TEXT NOT NULL,
      device_info TEXT,
      ip_address TEXT,
      is_active INTEGER DEFAULT 1,
      expires_at TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS login_attempts (
      id TEXT PRIMARY KEY,
      username TEXT NOT NULL,
      success INTEGER DEFAULT 0,
      ip_address TEXT,
      device_info TEXT,
      attempt_time TEXT DEFAULT (datetime('now')),
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== CURRENCIES =====================
  static final List<String> _currencyTables = [
    '''CREATE TABLE IF NOT EXISTS currencies (
      id TEXT PRIMARY KEY,
      code TEXT UNIQUE NOT NULL,
      name TEXT NOT NULL,
      fractional_unit TEXT,
      decimal_places INTEGER DEFAULT 2,
      exchange_rate TEXT DEFAULT '1',
      is_base INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS exchange_rates (
      id TEXT PRIMARY KEY,
      from_currency_code TEXT NOT NULL REFERENCES currencies(code),
      to_currency_code TEXT NOT NULL REFERENCES currencies(code),
      rate TEXT NOT NULL,
      effective_date TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now'))
    )''',
  ];

  // ===================== COMMISSION =====================
  static final List<String> _commissionTables = [
    '''CREATE TABLE IF NOT EXISTS sales_targets (
      id TEXT PRIMARY KEY,
      user_id TEXT REFERENCES users(id),
      target_amount TEXT DEFAULT '0',
      period TEXT NOT NULL,
      year INTEGER NOT NULL,
      month INTEGER,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS sales_commissions (
      id TEXT PRIMARY KEY,
      user_id TEXT REFERENCES users(id),
      sale_id TEXT REFERENCES sales(id),
      commission_rate TEXT DEFAULT '0',
      commission_amount TEXT DEFAULT '0',
      status TEXT DEFAULT 'PENDING',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== ADVANCED ACCOUNTING =====================
  static final List<String> _advancedTables = [
    '''CREATE TABLE IF NOT EXISTS acc_budgets (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      fiscal_year INTEGER NOT NULL,
      total_amount TEXT DEFAULT '0',
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS acc_bank_statements (
      id TEXT PRIMARY KEY,
      account_id TEXT NOT NULL REFERENCES gl_accounts(id),
      statement_date TEXT NOT NULL,
      opening_balance TEXT DEFAULT '0',
      closing_balance TEXT DEFAULT '0',
      reference_number TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS acc_bank_statement_lines (
      id TEXT PRIMARY KEY,
      bank_statement_id TEXT NOT NULL REFERENCES acc_bank_statements(id),
      transaction_date TEXT NOT NULL,
      description TEXT,
      debit TEXT DEFAULT '0',
      credit TEXT DEFAULT '0',
      reference TEXT,
      reconciled INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS acc_audit_logs (
      id TEXT PRIMARY KEY,
      user_id TEXT,
      action TEXT NOT NULL,
      table_name TEXT NOT NULL,
      record_id TEXT,
      old_values TEXT,
      new_values TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== ZAKAT & EOSB =====================
  static final List<String> _zakatEosbTables = [
    '''CREATE TABLE IF NOT EXISTS zakat_calculations (
      id TEXT PRIMARY KEY,
      year INTEGER NOT NULL,
      total_assets TEXT DEFAULT '0',
      total_liabilities TEXT DEFAULT '0',
      zakat_base TEXT DEFAULT '0',
      zakat_due TEXT DEFAULT '0',
      zakat_rate TEXT DEFAULT '0.025',
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS end_of_service_benefits (
      id TEXT PRIMARY KEY,
      employee_id TEXT NOT NULL REFERENCES hr_employees(id),
      calculation_date TEXT NOT NULL,
      total_service_years REAL DEFAULT 0,
      total_benefit TEXT DEFAULT '0',
      status TEXT DEFAULT 'DRAFT',
      notes TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== APP CONFIG =====================
  static final List<String> _appConfigTables = [
    '''CREATE TABLE IF NOT EXISTS app_config_table (
      key TEXT PRIMARY KEY,
      value TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS app_settings (
      id TEXT PRIMARY KEY,
      key TEXT UNIQUE NOT NULL,
      value TEXT NOT NULL,
      group_name TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
    '''CREATE TABLE IF NOT EXISTS inventory_reservations (
      id TEXT PRIMARY KEY,
      product_id TEXT NOT NULL REFERENCES products(id),
      warehouse_id TEXT NOT NULL REFERENCES warehouses(id),
      batch_id TEXT REFERENCES product_batches(id),
      quantity TEXT DEFAULT '0',
      reference_type TEXT NOT NULL,
      reference_id TEXT NOT NULL,
      status TEXT DEFAULT 'ACTIVE',
      expires_at TEXT,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now')),
      device_id TEXT,
      sync_status INTEGER DEFAULT 1,
      branch_id TEXT REFERENCES branches(id)
    )''',
  ];

  // ===================== INDEXES =====================
  static final List<String> _performanceIndexes = [
    'CREATE INDEX IF NOT EXISTS products_sku_idx ON products (sku)',
    'CREATE INDEX IF NOT EXISTS products_barcode_idx ON products (barcode)',
    'CREATE INDEX IF NOT EXISTS products_category_id_idx ON products (category_id)',
    'CREATE INDEX IF NOT EXISTS products_supplier_id_idx ON products (supplier_id)',
    'CREATE INDEX IF NOT EXISTS products_is_active_idx ON products (is_active)',
    'CREATE INDEX IF NOT EXISTS product_units_product_id_idx ON product_units (product_id)',
    'CREATE INDEX IF NOT EXISTS product_units_barcode_idx ON product_units (barcode)',
    'CREATE INDEX IF NOT EXISTS product_batches_product_warehouse_idx ON product_batches (product_id, warehouse_id)',
    'CREATE INDEX IF NOT EXISTS product_batches_expiry_date_idx ON product_batches (expiry_date)',
    'CREATE INDEX IF NOT EXISTS sale_items_sale_id_idx ON sale_items (sale_id)',
    'CREATE INDEX IF NOT EXISTS sale_items_product_id_idx ON sale_items (product_id)',
    'CREATE INDEX IF NOT EXISTS sales_customer_id_idx ON sales (customer_id)',
    'CREATE INDEX IF NOT EXISTS sales_created_at_idx ON sales (created_at)',
    'CREATE INDEX IF NOT EXISTS sales_status_idx ON sales (status)',
    'CREATE INDEX IF NOT EXISTS purchase_items_purchase_id_idx ON purchase_items (purchase_id)',
    'CREATE INDEX IF NOT EXISTS purchase_items_product_id_idx ON purchase_items (product_id)',
    'CREATE INDEX IF NOT EXISTS purchases_supplier_id_idx ON purchases (supplier_id)',
    'CREATE INDEX IF NOT EXISTS purchases_date_idx ON purchases (date)',
    'CREATE INDEX IF NOT EXISTS purchases_status_idx ON purchases (status)',
    'CREATE INDEX IF NOT EXISTS gl_entries_date_idx ON gl_entries (date)',
    'CREATE INDEX IF NOT EXISTS gl_entries_reference_idx ON gl_entries (reference_type, reference_id)',
    'CREATE INDEX IF NOT EXISTS gl_entries_status_idx ON gl_entries (status)',
    'CREATE INDEX IF NOT EXISTS gl_lines_entry_id_idx ON gl_lines (entry_id)',
    'CREATE INDEX IF NOT EXISTS gl_lines_account_id_idx ON gl_lines (account_id)',
    'CREATE INDEX IF NOT EXISTS gl_lines_cost_center_id_idx ON gl_lines (cost_center_id)',
    'CREATE INDEX IF NOT EXISTS stock_movements_product_id_idx ON stock_movements (product_id)',
    'CREATE INDEX IF NOT EXISTS stock_movements_reference_id_idx ON stock_movements (reference_id)',
    'CREATE INDEX IF NOT EXISTS stock_movements_movement_date_idx ON stock_movements (movement_date)',
    'CREATE INDEX IF NOT EXISTS stock_movements_type_idx ON stock_movements (type)',
    'CREATE INDEX IF NOT EXISTS customer_payments_customer_id_idx ON customer_payments (customer_id)',
    'CREATE INDEX IF NOT EXISTS customer_payments_payment_date_idx ON customer_payments (payment_date)',
    'CREATE INDEX IF NOT EXISTS supplier_payments_supplier_id_idx ON supplier_payments (supplier_id)',
    'CREATE INDEX IF NOT EXISTS supplier_payments_payment_date_idx ON supplier_payments (payment_date)',
    'CREATE INDEX IF NOT EXISTS audit_logs_timestamp_idx ON audit_logs (timestamp)',
    'CREATE INDEX IF NOT EXISTS audit_logs_target_entity_idx ON audit_logs (target_entity)',
    'CREATE INDEX IF NOT EXISTS sync_queue_status_idx ON sync_queue (status)',
    'CREATE INDEX IF NOT EXISTS inventory_transactions_product_warehouse_idx ON inventory_transactions (product_id, warehouse_id)',
    'CREATE INDEX IF NOT EXISTS inventory_transactions_reference_id_idx ON inventory_transactions (reference_id)',
    'CREATE INDEX IF NOT EXISTS account_transactions_reference_id_idx ON account_transactions (reference_id)',
    'CREATE INDEX IF NOT EXISTS account_transactions_reconciled_idx ON account_transactions (reconciled)',
  ];
}
