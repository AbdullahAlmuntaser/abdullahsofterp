# تقرير تنفيذ نظام الوحدات - SystemMarket

## ما تم تعديله:

### 1. قاعدة البيانات - Products Table
- **ملف:** `lib/data/datasources/local/tables/core_tables.dart`
- أضفت حقلين جديدين في جدول `Products`:
  - `unitsPerMainUnit` - عدد الحبات/الوحدات داخل الوحدة الرئيسية (معامل التعبئة)
  - `unitSellPrice` - سعر الوحدة المفردة المحسوب تلقائيًا
- **ملف:** `lib/data/migrations/v60_to_v61.dart` - Migration جديد
- **ملف:** `lib/data/datasources/local/app_database.dart` - Schema version → 61

### 2. شاشة إضافة/تعديل المنتج
- **ملف:** `lib/presentation/features/products/widgets/add_edit_product_dialog.dart`
- الحقول المضافة:
  - **الوحدة الرئيسية** - حقل إدخال نصي (كرتون، شدة، صندوق، باكت، كيس...)
  - **العدد** - كمية المخزون بالوحدة الرئيسية
  - **عدد الوحدات داخل الوحدة الرئيسية** - معامل التعبئة
  - **سعر الشراء للوحدة الرئيسية**
  - **سعر البيع بالجملة للوحدة الرئيسية**
  - **سعر البيع بالتجزئة للوحدة الرئيسية**
  - **سعر الوحدة المفردة** - يُحسب تلقائيًا (سعر التجزئة ÷ عدد الوحدات)
- لا توجد قيم ثابتة - كلها حقول إدخال يكتبها المستخدم

### 3. إصلاح مشكلة المخزون المضاعف
- **ملف:** `lib/core/services/transaction_engine.dart`
- **المشكلة:** عند شراء 10 كراتين (unitFactor=20)، كان النظام يُضاعف الكمية مرتين:
  - `GRN service` يضيف 10 للstocks
  - `TransactionEngine` يضيف 10×20 = 200
  - النتيجة: stocks = 210 بدلاً من 10
- **الحل:** أزلت تحديث stocks من TransactionEngine لأن GRN service يتعامل معها بالفعل. الآن stock يتحدث مرة واحدة فقط من خلال GRN

### 4. إصلاح بيع الوحدات
- **ملف:** `lib/core/services/transaction_engine.dart`
- بيع الوحدة الرئيسية: يبحث عن دفعات `storedUnitId = product.unit`
- بيع الوحدة المفردة: يبحث عن دفعات `storedUnitId = null`
- لا يوجد ضرب في `unitFactor` - الكمية تُخصم كما هي

### 5. فاتورة المشتريات
- **ملف:** `lib/presentation/features/purchases/add_purchase_page.dart`
- `quantityInBaseUnit` = الكمية كما أدخلها المستخدم (بدون ضرب في factor)
- **ملف:** `lib/presentation/features/purchases/widgets/purchase_item_row.dart`
- عند تغيير الوحدة، السعر يأخذ من سعر الشراء للوحدة المحددة
- **ملف:** `lib/presentation/features/purchases/purchase_provider.dart`
- `addItem` يستخدم سعر الشراء للوحدة الرئيسية مباشرة

### 6. فاتورة المبيعات
- **ملف:** `lib/presentation/features/sales/widgets/sales_item_row.dart`
- أضفت قائمة منسدلة لاختيار الوحدة (الرئيسية أو وحدات إضافية)
- السعر يتغير تلقائيًا حسب الوحدة المختارة
- يدعم وضع الجملة والتجزئة
- **ملف:** `lib/presentation/features/sales/sales_invoice_page.dart`
- الباركود يستخدم السعر الصحيح حسب وضع البيع (جملة/تجزئة)

### 7. صفحة التفكيك
- **ملف:** `lib/presentation/features/products/decomposition_page.dart` (جديد)
- المستخدم يختار منتج ويدخل كمية التفكيك
- النظام يستخدم `unitsPerMainUnit` من بيانات المنتج
- مثال: تفكيك 3 كرتون (محتوى 20) → تنقص 3 كرتون وتضاف 60 حبة
- يتم تسجيل حركات المخزون (DISASSEMBLY)

### 8. عرض المخزون
- **ملف:** `lib/core/utils/stock_display_adapter.dart`
- دعم عرض المخزون بالوحدة الرئيسية
- دعم عرض المخزون المفكك (وحدة رئيسية + حبات)

### 9. الباركود
- الباركود يرتبط بالمنتج和他的 الوحدة
- عند مسح باركود الوحدة الرئيسية → يبيع بالوحدة الرئيسية
- عند مسح باركود وحدة إضافية → يبيع بهذه الوحدة بالسعر الصحيح

## نتائج الاختبارات:
- `flutter analyze` - **0 أخطاء** (3 معلومات فقط - code style)
- تم توليد الكود المطلوب (build_runner)
- جميع الملفات المصابة تعمل بشكل صحيح

## ملخص كيف يعمل النظام:

| العملية | الطريقة |
|---------|---------|
| **إدخال منتج** | المستخدم يكتب: الوحدة (كرتون)، العدد (10)، المحتوى (20)، الأسعار |
| **شراء 10 كراتين** | stocks = 10 كراتين (لا ضرب في 20) |
| **تفكيك 1 كرتون** | تنقص 1 كرتون من stocks، تضاف 20 حبة |
| **بيع وحدة رئيسية** | تنقص 1 كرتون من stocks |
| **بيع حبة** | تنقص 1 حبة من المخزون المفكك |
| **السعر المفرد** | = سعر التجزئة ÷ عدد الوحدات (محسوب تلقائيًا) |
